general {
	interval = 5
}

order += "disk /"
order += "ethernet eth0"
order += "load"
order += "time"

ethernet eth0 {
	format_up = "%ip"
	format_down = "eth down"
}

time {
	format = "v.%U %Y-%m-%d %a %H:%M:%S"
}

load {
	format = "%5min"
}

disk "/" {
	format = "%free"
}
